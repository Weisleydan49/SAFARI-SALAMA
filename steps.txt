# Mobile app
flutter create matatu_app

# Backend
mkdir matatu_backend
cd matatu_backend
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
Run the following command to install FastAPI, Uvicorn, SQLAlchemy, and the PostgreSQL driver:
 pip install fastapi uvicorn sqlalchemy psycopg2-binary.
